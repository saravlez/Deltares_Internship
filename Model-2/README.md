



## Large Files Notice

One surrogate model exceeds GitHub’s 100 MB limit. Because of this, the file is **not stored directly in the repository**.  
All other surrogate model files are saved normally on `data/` 
